[project]
name = "relay-composite-utility"
authors = ["Stacks Developer"]
description = "A decentralized relay mechanism for composite goal tracking"
telemetry = false
requirements = []
[contracts.relay-composite]
path = "contracts/relay-composite.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
